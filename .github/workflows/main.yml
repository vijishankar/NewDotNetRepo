name: cleanup-old-branches
on: [push]
jobs:
  cleanup-old-branches:
    runs-on: ubuntu-latest
    steps:
      - uses: cultureamp/delete-old-branches-action@v0.3
        with:
          repo-token: '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'
          min-num-branches: 20
          days-to-keep-branches: 14
